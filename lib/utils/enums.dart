enum LoadingState { idle, busy }

enum Method { get, post, put, delete }

enum UserRole { tenant, owner, vendor }

enum LanguageCode { en , ar }

enum UpcomingServiceStatus { tracking, inProgress, completed }

enum SortType { price, rating, bestDeals }

enum DistanceFilter { near1Km, near5Km, near10Km }

enum QuotationStatus { awaitingApproval, approved, rejected }

enum EmployeeStatus { available, busy, unavailable }

enum CustomerAction { none, siteVisit, requestQuotation }

enum JobPhase { assign, inProgress, completed }
